import React from 'react';

const RegisterPage = () => {
  const containerStyle = {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
    backgroundColor: '#f6f8fa'
  };

  return (
      <div style={containerStyle}>
        <p>Register Page Working</p>
      </div>
  );
};

export default RegisterPage;