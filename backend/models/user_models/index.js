const { users } = require('./users');
const { supervisor } = require('./supervisor');
const { health_worker } = require('./health_worker');
const { village } = require('./village');
const { asha } = require('./asha');

module.exports = {
  users,
  supervisor,
  health_worker,
  village,
  asha
};
