rch_portal/
│
├── src/                         # Main source code of the web application
│
├── backend/                     # All backend server-side logic and services
│   ├── api/                     # REST API endpoints (routes for each page)
│   │   ├── auth/                # Handles login, logout, password change
│   │   ├── ec/                  # Eligible Couple page endpoints
│   │   ├── pw/                  # Pregnant Woman page endpoints
│   │   ├── child/               # Child page endpoints (registration, immunization)
│   │   ├── dashboard/           # Dashboard data endpoints (counts, summaries)
│   │   └── mother/              # Mother-view specific APIs (to be implemented later)
│   │
│   ├── controllers/             # Core business logic for each route (validations, workflows)
│   │   ├── auth_controller/
│   │   ├── ec_controller/
│   │   ├── pw_controller/
│   │   ├── child_controller/
│   │   └── dashboard_controller/
│   │
│   ├── models/                  # Database schema definitions (tables)
│   │   ├── user_models/         # users, supervisor, health_worker, village, asha
│   │   ├── ec_models/           # eligible_couple, ec_visit
│   │   ├── pw_models/           # pregnant_women, anc, delivery, infant, pnc
│   │   └── child_models/        # child, immunization
│   │
│   ├── services/                # Reusable backend services (e.g., auth service, data access layer)
│   │   ├── auth_service/
│   │   ├── ec_service/
│   │   ├── pw_service/
│   │   ├── child_service/
│   │   └── dashboard_service/
│   │
│   ├── utils/                   # Utility scripts (validators, formatters, etc.)
│   └── db/                      # Database connection, migrations, seeds
│
├── frontend/                    # All frontend (client-side) code
│   ├── pages/                   # Each web page corresponds to a folder
│   │   ├── login_page/          # Login & OTP-based mother access UI
│   │   ├── dashboard_page/      # Main dashboard UI after login
│   │   ├── ec_page/             # Eligible Couple registration & update pages
│   │   ├── pw_page/             # Pregnant Woman registration & follow-up pages
│   │   ├── child_page/          # Child registration & immunization pages
│   │   ├── change_password_page/# Password change UI
│   │   └── mother_view_page/    # Mother’s view (future implementation)
│   │
│   ├── components/              # Shared UI components (buttons, forms, modals, navbars)
│   ├── hooks/                   # Reusable frontend logic (API calls, auth checks, etc.)
│   ├── context/                 # React context providers (e.g., AuthContext)
│   ├── services/                # Frontend-side API service wrappers
│   └── styles/                  # CSS/SCSS files organized per module
│
├── tests/                       # All unit, integration, and end-to-end tests
│   ├── backend/                 # Tests for backend services and APIs
│   ├── frontend/                # Tests for UI components and pages
│   └── integration/             # Full-flow integration tests (e.g., EC → PW → Child)
│
├── docs/                        # Documentation
│   ├── db_schema/               # ER diagrams, schema explanations
│   ├── api_docs/                # API specifications and usage examples
│   └── architecture/            # System architecture, design decisions
│
├── scripts/                     # Deployment, migration, seeding, or data import scripts
│
├── config/                      # Environment variables, database configs, etc.
│
└── logs/                        # Application logs (for production/debugging)
